# Carrier Intelligence Agency

Pre-launch waitlist page. Deploys to Vercel as a static site.

## Deploy

1. Push to GitHub
2. Import repo in Vercel — it will auto-detect as a static site
3. No build step required

## Files

- `index.html` — the waitlist page
- `vercel.json` — clean URLs config
